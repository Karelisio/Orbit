<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget "Journal" : la dernière note du couple, tronquée si besoin, tenant
  sur une seule ligne de la grille du lanceur (voir widget_journal_info.xml).
  Le contenu prend toute la place restante (layout_weight) au lieu d'être en
  wrap_content : le pied de note reste ainsi toujours visible, quelle que
  soit la hauteur réelle que le lanceur accorde à la tuile.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:focusable="false"
        android:importantForAccessibility="no"
        android:scaleType="fitXY" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:paddingTop="8dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/widget_journal_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📝 Journal"
            android:textSize="11sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_journal_content"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="3dp"
            android:textSize="12sp"
            android:maxLines="4"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widget_journal_footer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textSize="10sp"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>
</FrameLayout>
