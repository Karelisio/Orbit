<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget "Fusion" : prochain événement + tâches + dernière note, tenant sur
  une seule ligne de la grille du lanceur (voir widget_combined_info.xml).
  Le contenu reste aligné en haut : si la tuile est plus courte que prévu,
  seule la dernière ligne est rognée, jamais la première.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/widget_background_solid"
        android:backgroundTint="@color/widget_dyn_primary_container"
        android:clickable="false"
        android:focusable="false"
        android:importantForAccessibility="no"
        android:scaleType="fitXY" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:paddingTop="8dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/widget_combined_event"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="📅 —"
            android:textColor="@color/widget_dyn_on_primary_container"
            android:textSize="12sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widget_combined_tasks"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:text="✅ —"
            android:textColor="@color/widget_dyn_on_primary_container"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widget_combined_journal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:text="📝 —"
            android:textColor="@color/widget_dyn_on_primary_container"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
